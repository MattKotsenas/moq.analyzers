# Playground

This is a playground so you can try out the analyzer without needing to create the package first.

Note that analyzers are cached in process, so you'll likely need to restart Visual Studio / Rider for your new changes to take effect.
